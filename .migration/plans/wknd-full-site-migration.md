I'm on the `main` branch.

To confirm:You're on the **`main`** branch.

Note: the DA Library block-path fix I made earlier was committed to the **`add-sidekick-block-library`** branch (PR #4), not `main` — that's where the `tools/sidekick/library.json` manifest lives. The live fix (uploaded to DA) is already active regardless of branch. Your working tree on `main` has the usual uncommitted plan doc plus a couple of untracked temp dirs (`.da-config/`, `.lib-fix/`).
